name: Unit Tests

run-name: "${{ github.event.inputs.title }}"

defaults:
  run:
    shell: bash -le {0}

on:
  repository_dispatch:
  workflow_dispatch:
    inputs:
      title:
        description: 'set a title for this run'
        required: false
        default: ''
      repo:
        description: 'GitHub repo {owner}/{repo}'
        required: false
        default: ''
      ref:
        description: 'GitHub ref: Branch, Tag or Commit SHA'
        required: false
        default: ''
      pr_number:
        description: 'PR Number'
        required: false
        type: number
      test_regex:
        description: 'Regex to filter test files'
        required: false
        default: ''
      max-parallel:
        description: 'Parallel jobs'
        required: false
        default: '4'
      model-test-mode:
        description: 'Model compat test mode'
        type: choice
        required: false
        default: 'fast'
        options:
          - 'fast'
          - 'slow'

env:
  CUDA_DEVICE_ORDER: PCI_BUS_ID
  CUDA_VISIBLE_DEVICES: 0
  # CI can allocate A100-class devices (sm_80), so the shared JIT arch list
  # must include 8.0 in addition to the newer targets.
  TORCH_CUDA_ARCH_LIST: '8.0 8.6 8.9 9.0 12.0'
  PYTORCH_ALLOC_CONF: 'expandable_segments:True'
  RUNNER: 10.0.13.31
  XEON5: 10.0.14.249
  LOGBAR_ANIMATION: '0'
  CUDA_VERSION: 130
  UV_TORCH_BACKEND: cu130
  TORCH_VERSION: 2.11.0
  # vllm doesn't support 3.14
  PYTHON_VERSION: 3.13
  UV_PYTHON: python3.13
  # PYTHON_GIL: 0 // test libs don't support yet
  IGNORED_TEST_FILES: "test_tgi.py,test_gptneox.py,models/test_mixtral.py,models/test_phi_3_moe.py,test_bits_new.py,models/test_internlm.py,models/test_internlm2_5.py,models/test_xverse.py"
  MODEL_TEST_MODE: ${{ github.event.inputs['model-test-mode'] || 'fast' }}
  MODEL_TEST_GPU_COUNT: '1'
  DEFUSER_GIT_URL: git+https://github.com/modelcloud/Defuser.git
  PYPCRE_GIT_URL: git+https://github.com/modelcloud/PyPcre.git
  TOKENICER_GIT_URL: git+https://github.com/modelcloud/Tokenicer.git
  LOGBAR_GIT_URL: git+https://github.com/modelcloud/LogBar.git
  EVALUTION_GIT_URL: git+https://github.com/modelcloud/Evalution.git
  repo: ${{ github.event.inputs.repo || github.repository }}
  ref: ${{ github.event.inputs.ref || github.ref }}
  HF_TOKEN: ${{ secrets.HF_TOKEN }}

concurrency:
  group: ${{ github.event.inputs.ref || github.ref }}-workflow-unit-tests #-${{ github.event.inputs.test_names }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  check-vm:
    runs-on: ubuntu-latest
    outputs:
      ip: ${{ steps.get_ip.outputs.ip }}
      run_id: ${{ steps.get_ip.outputs.run_id }}
      install_ts: ${{ steps.get_ip.outputs.install_ts }}
      max-parallel: ${{ steps.get_ip.outputs['max-parallel'] }}
      cuda_version: ${{ env.CUDA_VERSION }}
    steps:
      - name: Print env
        run: |
          echo "repo: ${{ env.repo }}"
          echo "ref: ${{ env.ref }}"

      - name: Checkout Codes
        uses: actions/checkout@v6
        with:
          repository: ${{ env.repo }}
          ref: ${{ env.ref }}

      - name: Set runner metadata
        id: get_ip
        run: |
          python3 .github/scripts/ci_workflow.py check-vm \
            --runner-ip "$RUNNER" \
            --github-run-id "${{ github.run_id }}" \
            --max-parallel "${{ github.event.inputs['max-parallel'] }}"

  list-test-files:
    runs-on: ubuntu-latest
    outputs:
      torch-files: ${{ steps.files.outputs.torch-files }}
      model-files: ${{ steps.files.outputs.model-files }}
      m4-files: ${{ steps.files.outputs.m4-files }}
      torch-envs: ${{ steps.files.outputs.torch-envs }}
      model-envs: ${{ steps.files.outputs.model-envs }}

    steps:
      - name: Checkout Codes
        uses: actions/checkout@v6
        with:
          repository: ${{ env.repo }}
          ref: ${{ env.ref }}

      - name: Fetch PR by number
        if: ${{ github.event.inputs.pr_number != 0 }}
        run: |
          PR_NUMBER=${{ github.event.inputs.pr_number }}
          echo "pr number $PR_NUMBER"
          git config --global --add safe.directory $(pwd)
          git fetch origin pull/${PR_NUMBER}/head:pr-${PR_NUMBER}
          git checkout pr-${PR_NUMBER}

      - name: List files
        id: files
        run: |
          python3 .github/scripts/ci_workflow.py list-tests \
            --ignored-test-files "$IGNORED_TEST_FILES" \
            --test-regex "${{ github.event.inputs.test_regex }}"

  prepare:
    needs:
      - list-test-files
      - check-vm
    runs-on: [ self-hosted, xeon5 ]
    container:
      image: ${{ needs.check-vm.outputs.ip }}:5000/nvidia/cuda:${{ needs.check-vm.outputs.cuda_version }}-ubuntu24.04_0325
      options: --device /dev/dri --ipc=host --runtime=nvidia --gpus all
      volumes:
        - /monster/ci/env/entrypoint.sh:/entrypoint.sh
        - /monster/ci/env/entrypoint.sh:/etc/profile.d/01-entrypoint.sh
        - /dev/dri/by-path:/dev/dri/by-path
        - /monster/ci/models:/monster/data/model
        - /monster/ci/dataset:/monster/data/model/dataset
        - /monster/ci/huggingface:/github/home/.cache/huggingface
        - /monster/ci/uv:/opt/uv
        - /monster/ci/env:/opt/env
        - /monster/ci/dist:/opt/dist
    if: always() && !cancelled() && (needs.list-test-files.outputs.torch-envs != '[]' || needs.list-test-files.outputs.model-envs != '[]')
    steps:
      - name: Checkout Codes
        uses: actions/checkout@v6
        with:
          repository: ${{ env.repo }}
          ref: ${{ env.ref }}

      - name: Fetch PR by number
        if: ${{ github.event.inputs.pr_number != 0 }}
        run: |
          PR_NUMBER=${{ github.event.inputs.pr_number }}
          echo "pr number $PR_NUMBER"
          git config --global --add safe.directory $(pwd)
          git fetch origin pull/${PR_NUMBER}/head:pr-${PR_NUMBER}
          git checkout pr-${PR_NUMBER}

      - name: Prepare common envs
        run: |
          source /opt/uv/setup_uv_venv.sh gptqmodel_unit_pre_env
          uv pip install pyyaml -U
          python3 .github/scripts/ci_workflow.py prepare-common-envs \
            --torch-envs-json '${{ needs.list-test-files.outputs.torch-envs }}' \
            --model-envs-json '${{ needs.list-test-files.outputs.model-envs }}' \
            --cuda-version "${{ needs.check-vm.outputs.cuda_version }}" \
            --torch-version "${{ env.TORCH_VERSION }}" \
            --runner "$RUNNER" \
            --repo-url "$DEFUSER_GIT_URL" \
            --repo-url "$PYPCRE_GIT_URL" \
            --repo-url "$TOKENICER_GIT_URL" \
            --repo-url "$LOGBAR_GIT_URL" \
            --repo-url "$EVALUTION_GIT_URL"

  torch:
    needs:
      - list-test-files
      - check-vm
      - prepare
    runs-on: [ self-hosted, xeon5 ]
    container:
      image: ${{ needs.check-vm.outputs.ip }}:5000/nvidia/cuda:${{ needs.check-vm.outputs.cuda_version }}-ubuntu24.04_0325
      options: --device /dev/dri --ipc=host --runtime=nvidia --gpus all
      volumes:
        - /monster/ci/env/entrypoint.sh:/entrypoint.sh
        - /monster/ci/env/entrypoint.sh:/etc/profile.d/01-entrypoint.sh
        - /dev/dri/by-path:/dev/dri/by-path
        - /monster/ci/models:/monster/data/model
        - /monster/ci/dataset:/monster/data/model/dataset
        - /monster/ci/huggingface:/github/home/.cache/huggingface
        - /monster/ci/uv:/opt/uv
        # - /monster/ci/uv/setup_uv_venv.sh:/opt/uv/setup_uv_venv.sh
        # - /monster/ci/uv/uv:/opt/uv/uv
        # - /monster/ci/uv/uvx:/opt/uv/uvx
        # - /monster/ci/uv/env:/opt/uv/env
        # - /monster/ci/uv/uv.toml:/opt/uv/uv.toml
        - /monster/ci/env:/opt/env
        - /monster/ci/dist:/opt/dist
    strategy:
      fail-fast: false
      max-parallel: ${{ fromJson(needs.check-vm.outputs['max-parallel']).size || 20 }}
      matrix:
        test_script: ${{ fromJSON(needs.list-test-files.outputs.torch-files) }}
    if: always() && !cancelled() && needs.list-test-files.outputs.torch-files != '[]'
    steps:
      - name: Checkout Codes
        uses: actions/checkout@v6
        with:
          repository: ${{ env.repo }}
          ref: ${{ env.ref }}

      - name: Fetch PR by number
        if: ${{ github.event.inputs.pr_number != 0 }}
        run: |
          PR_NUMBER=${{ github.event.inputs.pr_number }}
          echo "pr number $PR_NUMBER"
          git config --global --add safe.directory $(pwd)
          git fetch origin pull/${PR_NUMBER}/head:pr-${PR_NUMBER}
          git checkout pr-${PR_NUMBER}

      - name: Activate uv env
        run: |
          source /opt/uv/setup_uv_venv.sh gptqmodel_unit_test_env
          python3 .github/scripts/ci_workflow.py activate-test-env \
            --group tests \
            --test-name "${{ matrix.test_script }}" \
            --write-gpu-count \
            --fix-safe-directory

      - name: Setup uv env
        if: env.HAS_SPECIFIC_DEPS == '1'
        run: |
          python3 .github/scripts/ci_workflow.py setup-specific-env \
            --group tests \
            --test-name "${{ matrix.test_script }}" \
            --cuda-version "${{ needs.check-vm.outputs.cuda_version }}" \
            --torch-version "${{ env.TORCH_VERSION }}"

      - name: Install package from source
        if: ${{ env.ENV_NAME != 'gptqmodel_unit_tests_common' && env.ENV_NAME != 'gptqmodel_unit_tests_models_common' }}
        run: |
          safe_ref=$(printf '%s' "${{ env.ref }}" | sed -e 's/[\\/]/_/g' -e 's/^refs_heads_//')
          python3 .github/scripts/ci_workflow.py install-package \
            --env-name "${ENV_NAME}_$safe_ref" \
            --install-key "${{ needs.check-vm.outputs.run_id }}_${{ needs.check-vm.outputs.install_ts }}" \
            --test-script "${{ matrix.test_script }}" \
            --job "${{ github.job }}" \
            --run-id "${{ needs.check-vm.outputs.run_id }}" \
            --runner "$RUNNER" \
            --uv-torch-backend "${{ env.UV_TORCH_BACKEND }}" \
            --no-build-isolation

      - name: Find suitable GPU
        if: ${{ !contains(matrix.test_script, 'ipex') && !contains(matrix.test_script, 'xpu') && !cancelled() }}
        run: |
          python3 .github/scripts/ci_gpu.py allocate \
            --base-url "http://${XEON5}" \
            --run-id "${{ github.run_id }}" \
            --test "${{ matrix.test_script }}" \
            --runner "${RUNNER_NAME:-unknown}" \
            --count "${{ env.GPU_COUNT }}"


      - name: Run tests
        run: |
          extra_args=()
          if [[ "${{ matrix.test_script }}" == *ipex* ]]; then
            extra_args+=(--clear-cuda)
          fi
          if [[ "${{ matrix.test_script }}" == *xpu* ]]; then
            extra_args+=(--clear-cuda --xpu-mode)
          fi
          python3 .github/scripts/ci_tests.py run \
            --base-url "http://${XEON5}" \
            --run-id "${{ github.run_id }}" \
            --test-script "${{ matrix.test_script }}" \
            --runner "${RUNNER_NAME}" \
            --gpu-id "${{ env.CUDA_VISIBLE_DEVICES }}" \
            "${extra_args[@]}"

      - name: Check log
        if: ${{ !cancelled() &&  failure() }}
        continue-on-error: true
        run: |
          python3 .github/scripts/ci_tests.py check-log \
            --run-id "${{ github.run_id }}" \
            --test-script "${{ matrix.test_script }}"

      - name: Release GPU
        if: always() && !contains(matrix.test_script, 'ipex') && !contains(matrix.test_script, 'xpu')
        run: |
          safe_ref=$(printf '%s' "${{ env.ref }}" | sed -e 's/[\\/]/_/g' -e 's/^refs_heads_//')

          python3 .github/scripts/ci_workflow.py cleanup-install-lock \
            --env-name "${ENV_NAME:-}_$safe_ref" \
            --install-key "${{ needs.check-vm.outputs.run_id }}_${{ needs.check-vm.outputs.install_ts }}"
          python3 .github/scripts/ci_gpu.py release \
            --base-url "http://${XEON5}" \
            --run-id "${{ github.run_id }}" \
            --gpu-id "${{ env.CUDA_VISIBLE_DEVICES }}" \
            --timestamp "${{ env.STEP_TIMESTAMP }}" \
            --test "${{ matrix.test_script }}" \
            --runner "${RUNNER_NAME}"

      - name: Clean cache
        if: always()
        run: |
          rm -rf rm -rf ./* .[^.] .??*

  torch-models:
    needs:
      - list-test-files
      - check-vm
      - prepare
    runs-on: [ self-hosted, xeon5 ]
    container:
      image: ${{ needs.check-vm.outputs.ip }}:5000/nvidia/cuda:${{ needs.check-vm.outputs.cuda_version }}-ubuntu24.04_0325
      options: --device /dev/dri --ipc=host --runtime=nvidia --gpus all
      volumes:
        - /monster/ci/env/entrypoint.sh:/entrypoint.sh
        - /monster/ci/env/entrypoint.sh:/etc/profile.d/01-entrypoint.sh
        - /dev/dri/by-path:/dev/dri/by-path
        - /monster/ci/models:/monster/data/model
        - /monster/ci/dataset:/monster/data/model/dataset
        - /monster/ci/huggingface:/github/home/.cache/huggingface
        - /monster/ci/uv:/opt/uv
        # - /monster/ci/uv/setup_uv_venv.sh:/opt/uv/setup_uv_venv.sh
        # - /monster/ci/uv/uv:/opt/uv/uv
        # - /monster/ci/uv/uvx:/opt/uv/uvx
        # - /monster/ci/uv/env:/opt/uv/env
        # - /monster/ci/uv/uv.toml:/opt/uv/uv.toml
        - /monster/ci/env:/opt/env
        - /monster/ci/dist:/opt/dist
    strategy:
      fail-fast: false
      max-parallel: ${{ fromJson(needs.check-vm.outputs['max-parallel']).size || 20 }}
      matrix:
        test_script: ${{ fromJSON(needs.list-test-files.outputs.model-files) }}
    if: always() && !cancelled() && needs.list-test-files.outputs.model-files != '[]'
    steps:
      - name: Checkout Codes
        uses: actions/checkout@v6
        with:
          repository: ${{ env.repo }}
          ref: ${{ env.ref }}

      - name: Fetch PR by number
        if: ${{ github.event.inputs.pr_number != 0 }}
        run: |
          PR_NUMBER=${{ github.event.inputs.pr_number }}
          echo "pr number $PR_NUMBER"
          git config --global --add safe.directory $(pwd)
          git fetch origin pull/${PR_NUMBER}/head:pr-${PR_NUMBER}
          git checkout pr-${PR_NUMBER}

      - name: Activate uv env
        run: |
          source /opt/uv/setup_uv_venv.sh gptqmodel_unit_test_env
          python3 .github/scripts/ci_workflow.py activate-test-env \
            --group tests/models \
            --test-name "${{ matrix.test_script }}"

      - name: Setup uv env
        if: env.HAS_SPECIFIC_DEPS == '1'
        run: |
          python3 .github/scripts/ci_workflow.py setup-specific-env \
            --group tests/models \
            --test-name "${{ matrix.test_script }}" \
            --cuda-version "${{ needs.check-vm.outputs.cuda_version }}" \
            --torch-version "${{ env.TORCH_VERSION }}"

      - name: Install package from source
        if: ${{ env.ENV_NAME != 'gptqmodel_unit_tests_common' && env.ENV_NAME != 'gptqmodel_unit_tests_models_common' }}
        run: |
          safe_ref=$(printf '%s' "${{ env.ref }}" | sed -e 's/[\\/]/_/g' -e 's/^refs_heads_//')

          python3 .github/scripts/ci_workflow.py install-package \
            --env-name "${ENV_NAME}_$safe_ref" \
            --install-key "${{ needs.check-vm.outputs.run_id }}_${{ needs.check-vm.outputs.install_ts }}" \
            --test-script "${{ matrix.test_script }}" \
            --job "${{ github.job }}" \
            --run-id "${{ needs.check-vm.outputs.run_id }}" \
            --runner "$RUNNER" \
            --uv-torch-backend "${{ env.UV_TORCH_BACKEND }}"

      - name: Find suitable GPU
        if: ${{ !cancelled() }}
        run: |
          python3 .github/scripts/ci_gpu.py allocate \
            --base-url "http://${XEON5}" \
            --run-id "${{ github.run_id }}" \
            --test "${{ matrix.test_script }}" \
            --runner "${RUNNER_NAME:-unknown}" \
            --count "1" \
            --require-single

      - name: Run tests
        run: |
          python3 .github/scripts/ci_tests.py run \
            --base-url "http://${XEON5}" \
            --run-id "${{ github.run_id }}" \
            --test-script "${{ matrix.test_script }}" \
            --runner "${RUNNER_NAME}" \
            --gpu-id "${{ env.CUDA_VISIBLE_DEVICES }}" \
            --model-test-mode "${MODEL_TEST_MODE}"

      - name: Check log
        if: ${{ !cancelled() && failure() }}
        continue-on-error: true
        run: |
          python3 .github/scripts/ci_tests.py check-log \
            --run-id "${{ github.run_id }}" \
            --test-script "${{ matrix.test_script }}"

      - name: Release GPU
        if: always()
        run: |
          safe_ref=$(printf '%s' "${{ env.ref }}" | sed -e 's/[\\/]/_/g' -e 's/^refs_heads_//')

          python3 .github/scripts/ci_workflow.py cleanup-install-lock \
            --env-name "${ENV_NAME:-}_$safe_ref" \
            --install-key "${{ needs.check-vm.outputs.run_id }}_${{ needs.check-vm.outputs.install_ts }}"
          python3 .github/scripts/ci_gpu.py release \
            --base-url "http://${XEON5}" \
            --run-id "${{ github.run_id }}" \
            --gpu-id "${{ env.CUDA_VISIBLE_DEVICES }}" \
            --timestamp "${{ env.STEP_TIMESTAMP }}" \
            --test "${{ matrix.test_script }}" \
            --runner "${RUNNER_NAME}"

      - name: Clean cache
        if: always()
        run: |
          rm -rf rm -rf ./* .[^.] .??*

  torch-test:
    runs-on: [ self-hosted, xeon5 ]
    container:
      image: 10.0.13.31:5000/nvidia/cuda:130-ubuntu24.04_0325
      options: --device /dev/dri --ipc=host --runtime=nvidia --gpus all
      volumes:
        - /monster/ci/env/entrypoint.sh:/entrypoint.sh
        - /monster/ci/env/entrypoint.sh:/etc/profile.d/01-entrypoint.sh
        - /dev/dri/by-path:/dev/dri/by-path
        - /monster/ci/uv:/opt/uv
        - /monster/ci/env:/opt/env
    steps:
      - name: Checkout Codes
        uses: actions/checkout@v6
        with:
          repository: ${{ env.repo }}
          ref: ${{ env.ref }}

      - name: Test pypi pip
        run: |
          uv venv pypi_pip_env
          source pypi_pip_env/bin/activate
          uv pip install pip -U
          pip install gptqmodel torch -U

      - name: Test pypi uv
        run: |
          uv venv pypi_uv_env
          source pypi_uv_env/bin/activate
          uv pip install gptqmodel torch -U

      - name: test local pip
        run: |
          uv venv local_pip_env
          source local_pip_env/bin/activate
          uv pip install pip -U
          pip install . torch -U

      - name: test local uv
        run: |
          uv venv local_uv_env
          source local_uv_env/bin/activate
          uv pip install . torch -U

  prepare-compat:
    runs-on: ubuntu-latest
    outputs:
      versions: ${{ steps.parser.outputs.versions || '[]' }}
    steps:
      - uses: actions/setup-python@v6
        with:
          python-version: "3.14"

      - name: Generate version matrix
        id: parser
        run: |
          versions=$(python .github/scripts/ci_loop_versions.py setuptools ">=77.0.1,<83")
          echo "versions=$versions" >> "$GITHUB_OUTPUT"

  check-compat:
    needs: prepare-compat
    if: needs.prepare.outputs.versions != '[]'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        version: ${{ fromJSON(needs.prepare.outputs.versions) }}

    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.14"
          cache: pip

      - name: Install package with selected setuptools
        run: |
          python -m pip install --upgrade pip
          python -m pip install . "setuptools==${{ matrix.version }}"

      - name: Show versions
        run: |
          python --version
          python -m pip show setuptools
